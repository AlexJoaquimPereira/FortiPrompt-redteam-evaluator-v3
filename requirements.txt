# FortiPrompt v3 — requirements.txt

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# HTTP client (calls llama.cpp server)
httpx>=0.27.0

# MongoDB persistence (optional — only needed if MONGO_URI is set)
pymongo>=4.6.0
